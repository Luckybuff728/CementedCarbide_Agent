# ============================================
# TopMat Agent 后端依赖（精简优化版）
# 优化：删除未使用的langchain和langchain-community
# 减少约100-120 MB
# ============================================

# 核心LLM框架（只保留实际使用的）
langchain-core>=0.3.75,<2.0.0  # 核心抽象类（BaseMessage等）
langgraph>=1.0.0                # 工作流编排

# 阿里云百炼LLM
dashscope==1.20.0

# Web框架
fastapi==0.115.0
uvicorn==0.31.0
websockets==13.0

# 数据处理 / ORM
pydantic==2.9.0
sqlalchemy==2.0.36

# 工具库
python-dotenv==1.0.0
aiofiles==24.1.0
httpx==0.27.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
