# ============================================
# TopMat Agent 后端依赖
# 已适配 LangChain/LangGraph v1.0
# ============================================

# 核心LLM框架（LangChain/LangGraph v1.0）
langchain>=1.0.0                # v1.0 新 API (create_agent)
langchain-core>=1.0.0           # 核心抽象类（BaseMessage等）
langgraph>=1.0.0                # 工作流编排 (InMemorySaver等)

# 阿里云百炼LLM
dashscope==1.20.0

# Web框架
fastapi==0.115.0
uvicorn==0.31.0
websockets==13.0

# 数据处理 / ORM
numpy>=1.23.2,<2           # 兼容 pandas 2.2.x
pandas>=2.2.0              # 数据处理
pydantic==2.9.0
sqlalchemy==2.0.36

# 工具库
python-dotenv==1.0.0
aiofiles==24.1.0
httpx==0.27.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
