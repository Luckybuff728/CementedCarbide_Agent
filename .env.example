# ============================================
# TopMat Agent 环境变量配置文件示例
# ============================================
# 使用说明:
# 1. 复制此文件为 .env
# 2. 填写必需的配置项（带*标记的）
# 3. 可选配置项可以使用默认值
# ============================================

# ========== LLM配置 ==========
# * 阿里云百炼API密钥（必需）
# 获取地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your_api_key_here

# LLM模型名称（可选，默认: qwen-plus）
# 可选值: qwen-max, qwen-plus, qwen-turbo
DASHSCOPE_MODEL_NAME=qwen-plus

# ========== 数据库配置 ==========
# 数据库连接URL（可选，默认使用SQLite）
# SQLite示例: sqlite:///./topmat.db
# PostgreSQL示例: postgresql://user:password@localhost/dbname
DATABASE_URL=sqlite:///./topmat.db

# ========== 服务器配置 ==========
# 服务器监听地址（可选，默认: 0.0.0.0）
# 本地开发可使用 localhost，生产环境建议 0.0.0.0
SERVER_HOST=0.0.0.0

# 服务器端口（可选，默认: 8000）
SERVER_PORT=8000

# ========== 日志配置 ==========
# 日志级别（可选，默认: INFO）
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ========== LangSmith 配置（可观测性） ==========
# * LangSmith API密钥（启用追踪必需）
# 获取地址: https://smith.langchain.com/
LANGSMITH_API_KEY=your_langsmith_api_key_here
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# 启用追踪（可选，默认: false）
# 设为 true 后，所有 LangGraph 调用将自动发送到 LangSmith
LANGSMITH_TRACING=false

# 项目名称（可选，默认: default）
# 用于在 LangSmith 中组织和筛选 Trace
LANGSMITH_PROJECT=topmat-coating-agent

# 工作区ID（可选，多工作区时使用）
# LANGSMITH_WORKSPACE_ID=

# ============================================
# 注意事项:
# - 请勿将 .env 文件提交到版本控制系统
# - API密钥等敏感信息请妥善保管
# - 生产环境建议使用更严格的配置
# ============================================
